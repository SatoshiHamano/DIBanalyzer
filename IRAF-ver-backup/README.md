# IRAF version backup

This directory is a historical snapshot of scripts from the older
IRAF-dependent workflow.

Use it as a reference archive when comparing old behavior or restoring an IRAF
specific implementation.  The active scripts are expected to live in the
repository root or in a future package layout.

